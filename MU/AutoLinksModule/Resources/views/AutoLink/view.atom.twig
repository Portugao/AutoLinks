{# purpose of this template: auto links atom feed #}
<?xml version="1.0" encoding="{% set charset = pageGetVar('meta.charset') %}{% if charset == 'ISO-8859-15' %}ISO-8859-1{% else %}{{ charset }}{% endif %}" ?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <title type="text">{{ __('Latest auto links') }}</title>
    <subtitle type="text">{{ __('A direct feed showing the list of auto links') }} - {{ getSystemVar('slogan') }}</subtitle>
    <author>
        <name>{{ getSystemVar('sitename') }}</name>
    </author>
{% set amountOfItems = items|length %}
{% if amountOfItems > 0 %}
{% set uniqueID %}tag:{{ app.request.schemeAndHttpHost|replace({'http://': '', '/': ''}) }},{{ items.first.createdDate|date('Y-m-d') }}:{{ path('muautolinksmodule_autolink_' ~ routeArea ~ 'view') }}{% endset %}
    <id>{{ uniqueID }}</id>
    <updated>{{ items[0].updatedDate|date('Y-m-dTH:M:SZ') }}</updated>
{% endif %}
    <link rel="alternate" type="text/html" hreflang="{{ app.request.locale }}" href="{{ url('muautolinksmodule_autolink_' ~ routeArea ~ 'index') }}" />
    <link rel="self" type="application/atom+xml" href="{{ app.request.schemeAndHttpHost ~ app.request.basePath }}" />
    <rights>Copyright (c) {{ 'now'|date('Y') }}, {{ app.request.schemeAndHttpHost }}</rights>
{% for autoLink in items %}
    <entry>
        <title type="html">{{ autoLink|muautolinksmodule_formattedTitle|notifyFilters('muautolinksmodule.filterhook.autolinks')|safeHtml }}</title>
        <link rel="alternate" type="text/html" href="{{ url('muautolinksmodule_autolink_' ~ routeArea ~ 'view') }}" />
        {% set uniqueID %}tag:{{ app.request.schemeAndHttpHost|replace({ 'http://': '', '/': '' }) }},{{ autoLink.createdDate|date('Y-m-d') }}:{{ path('muautolinksmodule_autolink_' ~ routeArea ~ 'view') }}{% endset %}
        <id>{{ uniqueID }}</id>
        {% if autoLink.updatedDate|default %}
            <updated>{{ autoLink.updatedDate|date('Y-m-dTH:M:SZ') }}</updated>
        {% endif %}
        {% if autoLink.createdDate|default %}
            <published>{{ autoLink.createdDate|date('Y-m-dTH:M:SZ') }}</published>
        {% endif %}
        {% if autoLink.createdBy|default and autoLink.createdBy.getUid() > 0 %}
            {% set creatorAttributes = autoLink.createdBy.getAttributes() %}
            <author>
               <name>{{ creatorAttributes.get('realname')|default(creatorAttributes.get('name'))|default(autoLink.createdBy.getUname()) }}</name>
               <uri>{{ creatorAttributes.get('_UYOURHOMEPAGE')|default('-') }}</uri>
               <email>{{ autoLink.createdBy.getEmail() }}</email>
            </author>
        {% endif %}
        <summary type="html">
            <![CDATA[
            {{ autoLink.supportedString|truncate(150, true, '&hellip;')|default('-') }}
            ]]>
        </summary>
        <content type="html">
            <![CDATA[
            {{ autoLink.neededLink|replace({ '<br>': '<br />' }) }}
            ]]>
        </content>
    </entry>
{% endfor %}
</feed>
